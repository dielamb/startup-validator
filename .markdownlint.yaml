default: true

# Allow long lines (tables, prompts often exceed 80 chars)
MD013: false

# Allow inline HTML (we use <div align="center"> for hero header)
MD033: false

# Allow files that don't start with H1 (templates use HTML wrapper)
MD041: false

# Allow same heading in different sections
MD024:
  siblings_only: true

# Allow bare URLs in tables/links
MD034: false
